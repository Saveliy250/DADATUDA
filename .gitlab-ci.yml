stages:
    - build
    - run
build:
    stage: build
    tags:
        - dind
    script:
        - docker build- t miniapp:latest .
run:
    stage: run
    tags: 
        - dind
    script:
        - docker run -p 5173:5173 miniapp:latest
